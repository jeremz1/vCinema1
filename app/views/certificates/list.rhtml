
<% for c in Certificate.find(:all) %>

	<%= link_to( c.name, :action => "show", :id => c.id) %> <%= link_to "[Delete]", :action => "delete", :id => c.id %><br>

<% end %>
<br>
<%= link_to "Add New Certificate", :action => "new" %>
